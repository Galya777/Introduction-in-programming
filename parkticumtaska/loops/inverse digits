#include <iostream>
#include<math.h>
using namespace std;
int main()
{
	int number;
	cin >> number;
	int digit;
	while (number > 0) {
	 digit = number % 10;
		cout << digit;
		number= number / 10;
	}
	
   return 0;
}
