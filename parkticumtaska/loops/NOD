#include <iostream>
#include <math.h>
using namespace std;
int main()
{
	int N,M;
	cin >> N>>M; 
	int max = 1;
	for (int i = 1; i <= N; i++) {
			if (N % i == 0 && M % i == 0) {
				max = i;
			}
		}
	cout << max;
 return 0;
}
