#include <iostream>
using namespace std;
int main()
{
	char letter;
	cout << "Enter a letter: ";
	cin >> letter;
	if (letter >= 'A' && letter <= 'Z') {
		cout << "BIG";
	}
	else if (letter >= 'a' && letter <= 'z') {
		cout << "small";
	}
	else {
		cout << "NOT A LETTER, STUPID!";
	}
   return 0;
}
